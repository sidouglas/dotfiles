- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.nanorc:
    ~/.fzf:
    ~/.gitconfig:
    ~/.zsh_custom:
    ~/.zshenv:
    ~/.zshrc:

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
    - ./setup_homebrew.zsh
    - ./setup_zsh.zsh
    - ./setup_node.zsh
    - ./setup_macos.zsh
    - ./setup_disable_spotlight.zsh
