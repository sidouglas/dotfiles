- defaults:
    stdout: true
    stderr: true
    link:
      relink: true

- clean: ['~']

- link:
    ~/.bash_profile:
    ~/.bashrc:
    ~/.boto:
    ~/.fzf:
    ~/.gitconfig:
    ~/.nanorc:
    ~/.profile:
    ~/.zlogin:
    ~/.zsh_custom:
    ~/.zshenv:
    ~/.zshrc:

- create:
    - ~/downloads
    - ~/.vim/undo-history

- shell:
    - ./setup_homebrew.zsh
